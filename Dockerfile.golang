FROM registry.cn-hangzhou.aliyuncs.com/eryajf/golang:1.21.9-alpine3.19

WORKDIR /app

COPY go.mod go.sum ./
RUN go mod download

COPY . .

# 确保编译输出到正确的位置
RUN CGO_ENABLED=0 GOOS=linux go build -o main .

# 确保文件有执行权限
RUN chmod +x /app/main

EXPOSE 8080

# 使用绝对路径确保命令正确
CMD ["/app/main"] 